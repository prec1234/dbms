CREATE TABLE Employee (
    emp_id INT PRIMARY KEY,
    name VARCHAR(50),
    salary INT,
    dept_id INT
);

INSERT INTO Employee VALUES 
(1,'parth',30000,1),
(2,'Sarthak',40000,2),
(3,'Fazal',35000,1);

SELECT * FROM Employee;

SET SQL_SAFE_UPDATES = 0;
UPDATE Employee SET salary = salary + 5000 WHERE dept_id = 1;

DELETE FROM Employee WHERE emp_id = 3;

SELECT * FROM Employee WHERE salary > 30000 AND dept_id = 1;

SELECT name, salary*12 AS annual_salary FROM Employee;

SELECT * FROM Employee WHERE name LIKE 'A%';

SELECT UPPER(name) FROM Employee;

SELECT dept_id FROM Employee
UNION
SELECT dept_id FROM Department;

CREATE USER 'user1'@'localhost' IDENTIFIED BY '1234';

CREATE ROLE 'manager';

GRANT SELECT, INSERT ON Parth.Employee TO 'user1'@'localhost';

GRANT 'manager' TO 'user1'@'localhost';

REVOKE INSERT ON Parth.Employee FROM 'user1'@'localhost';

START TRANSACTION;

INSERT INTO Employee VALUES (4,'Samarth',45000,2);

SAVEPOINT sp1;

UPDATE Employee SET salary = 50000 WHERE emp_id = 4;

ROLLBACK TO sp1;

COMMIT;